import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Storage is Node's built-in node:sqlite — no native module to compile and
  // nothing to mark external. Kept here as the place for future server-only deps.
};

export default nextConfig;
